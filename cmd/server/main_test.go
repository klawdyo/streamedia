package main

import "testing"

func TestMainPackageExists(t *testing.T) {
	// Verifica que o pacote main existe e compila.
	// Este teste trivial garante que o scaffold está no lugar.
	t.Log("pacote main existe e compila")
}
